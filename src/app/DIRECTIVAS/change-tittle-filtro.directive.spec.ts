import { ChangeTittleFiltroDirective } from './change-tittle-filtro.directive';

describe('ChangeTittleFiltroDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeTittleFiltroDirective();
    expect(directive).toBeTruthy();
  });
});
