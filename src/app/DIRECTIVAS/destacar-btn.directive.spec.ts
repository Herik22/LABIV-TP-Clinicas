import { DestacarBtnDirective } from './destacar-btn.directive';

describe('DestacarBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarBtnDirective();
    expect(directive).toBeTruthy();
  });
});
